# FastAPI backend
POSTGRES_DSN=postgresql://vendange:vendange@localhost:55432/vendange
# Optional tuning knobs (defaults are safe to omit)
POSTGRES_STATEMENT_TIMEOUT_MS=5000
POSTGRES_POOL_MIN_SIZE=1
POSTGRES_POOL_MAX_SIZE=10
ONTOP_ENDPOINT_URL=http://localhost:8080/sparql
